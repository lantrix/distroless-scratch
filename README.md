# Sample small containers

Compile and run a simple C Hello World app using Docker scratch

```shell
build-scratch.sh
```

Compile and run a simple C Hello World app using Distroless

```shell
build-distroless.sh
```
